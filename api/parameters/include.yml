in: query
name: include
description: Include related resources. Use a dot-seperated path to include relationships of relationships. Use a comma-deliminated list to sort by multiple attributes - first in list takes priority
schema:
  type: string
example: characters,staff.person
explode: false
