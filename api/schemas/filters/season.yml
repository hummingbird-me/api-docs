properties:
  season:
    $ref: ../../schemas/enums/season.yml
    description: |
      Filter by season. Use a comma-deliminated list to filter by multiple seasons

      Dec-Feb = Winter; Mar-May = Spring; Jun-Aug = Summer; Oct-Nov = Fall
