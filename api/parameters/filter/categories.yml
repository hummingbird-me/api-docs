in: query
name: filter[categories]
description: |
  Filter media by categories. Uses the categories' `slug` value. Use a comma-deliminated list to filter by multiple categories
schema:
  type: string
  example: comedy
