# Implements
#   filter[episodeCount]
#   filter[episodeLength]
type: object
properties:
  episodeCount:
    type: integer
    nullable: true
    minimum: 1

  episodeLength:
    type: integer
    format: minutes
    nullable: true
    minimum: 1
    description: Length of the episode in minutes

  totalLength:
    type: integer
    format: minutes
    nullable: true
    minimum: 1
    readOnly: true
