in: query
name: sort
description: Sort by ID or resource attributes. Ascending by default. Prefix with `-` to sort descending (e.g `-id`). Use a comma-deliminated list to sort by multiple attributes - first in list takes priority
schema:
  type: string
example: id
